(function () {
    'use strict';

    /** Defines the 'aboutView' module, which encapsulates the View and
     * Controller for the static About page.
     * The About view is a static HTML page, and consequently this module has no
     * dependencies and an empty Controller.
     */
    angular.module('aboutView', []);
})();
