(function () {
    'use strict';

    /** Defines the Category module, which handles all interaction with the server for Category objects.
     * - ngResource is required in order to interact with the server's REST API, by sending HTTP requests.
     */
    angular.module('core.category', ['ngResource']);
})();